[project]
name = "fastapi-fixture"
version = "1.0.0"
description = "FastAPI + LangChain E2E test fixture"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py310"
